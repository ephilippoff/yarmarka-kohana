{% extends "layouts/detail" %}

{% block leftside %}
    {% set object = { id: 4052585} %}

    <span class="button button-style1 bg-color-blue mr3 mb3 js-service-premium" data-id="{{object.id}}"><i class="fa fa-check-circle fa-lg mr5"></i>Премиум</span>
    <span class="button button-style1 bg-color-blue mr3 mb3 js-service-lider" data-id="{{object.id}}"><i class="fa fa-check-circle fa-lg mr5"></i>Лидер</span>
    <span class="button button-style1 bg-color-blue mr3 mb3 js-service-tglink" data-id="{{object.id}}"><i class="fa fa-check-circle fa-lg mr5"></i>Графическая ссылка</span>
    


    <% if (info.code > 200 && info.code < 400) { %>
        <%=info.text%>
    <% } %>

    <% if (info.code >= 400) { %>
        <p>Вы не можете воспользоваться этой услугой, потому что объявление заполнено не полностью.</p>
        <p><%=info.text%></p>
    <% } %>

{% endblock leftside %}