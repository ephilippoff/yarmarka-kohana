<style>
	#map {
		height:500px;
		width:100%;
		position: absolute;
		top:0;
	}

	.m-map__cont {
		position:relative;
		height:60px;.
		height: 52px;
		margin-bottom: 15px;
		-webkit-transition: height .5s ease,box-shadow .5s linear;
		transition: height .5s ease,box-shadow .5s linear;
		box-shadow: 0 1px 3px rgba(24,80,121,.6);
		overflow: hidden;
		z-index: 1;
	}

	.m-map__cont.expanded {
		height:440px;
	}


	.m-map__button-expand {
		position: absolute;
		top: 0;
		left: 0;
		width: 100%;
		height: 100%;
		border:0;
		line-height: 60px;
		color:black;
		font-size: 24px;
		overflow: hidden;
		background: white;
		opacity: 0.7;
		-webkit-transition: opacity .5s ease;
		transition: opacity .5s ease;
		z-index: 10;
	}

	.m-map__switch.hidden {
		z-index: 0;
		opacity: 0;
	}

	.m-map__button-collapse {
		display: inline;
		position: absolute;
		top:10px;
		right:50px;
		opacity: 0.7;
		background: white;
		padding:5px;
		border: 1px solid gray;
	}

	.m-map__button-collapse.hidden {
		display: none;
	}

	.m-map__count {
		display:inline;
		position: absolute;
		top:10px;
		left:10px;
		opacity: 0.7;
		background: white;
		padding:5px;
	}

	.m-map__count.hidden {
		display: none;
	}

	.m-map__baloon {
		width: 300px;
	}

	.m-map__baloon_left {
		display:block;
		width:80px;
		float:left;
	}

	.m-map__baloon_right {
		display:block;
		float:left;
		width:220px;
	}

</style>
<div class="info-block-cont theme3 module m-map__cont js-map-cont">
	
	{% if city %}
		{% set lat = city.geo_loc|split(',')[0] %}
		{% set lon = city.geo_loc|split(',')[1] %}
	{% endif %}
	<div id="map" class="js-map" data-lat="{{lat}}" data-lon="{{lon}}" ></div>
	<button type="button" class="m-map__button-expand bold js-map-expand">
		<i class="fa fa-map-o mr5"></i>Поиск на карте
	</button>
	<span class="m-map__count js-map-count hidden"> {{ (main_search_result_count > 1000) ? 1000 : main_search_result_count}} объектов из {{main_search_result_count}} </span>
	<button type="button" class="m-map__button-collapse js-map-collapse hidden">Закрыть</button>
</div>