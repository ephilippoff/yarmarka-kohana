{% extends "layouts/email" %}

{% block body %} 

<tr>
    <td colspan="2" style="padding:10px;">
        Вам было отправлено сообщение по объявлению:
        <a style="color: #d44234;" href="{{ email_link(domain, '/detail/' ~ object.id, ref_params, {'utm_content': 'object'}) }}">{{ object.title }}</a>
    </td>
</tr>
<tr>
    <td colspan="2" style="padding:10px;">
        
        <p>
            Текст сообщения: {{ message|e }}
        </p>
        <p>
            Email отправителя: <a href="mailto:{{ user.email }}">{{ user.email }}</a>
        </p>
    </td>
</tr>

{% endblock body %}
